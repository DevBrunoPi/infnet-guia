apiVersion: v1
kind: Service
metadata:
  name: guia-bruno-service
spec:
  type: NodePort
  selector:
    app: guia-bruno
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
